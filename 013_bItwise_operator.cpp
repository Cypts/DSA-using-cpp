#include<iostream>
using namespace std;
int main()
{
    int a,b;
    a=4,b=6;
    cout<<"bitwise and& "<<(a&b);
    cout<<"\nbitwise or | "<<(a|b);
    cout<<"\nbitwise XOR ^ "<<(a^b);
    cout<<"\nbitwise not "<<~a;
    
    
}